<h1>Listing posts</h1>

<table>
  <tr>
    <th>Uid</th>
    <th>Post</th>
    <th>Votes</th>
    <th>Root</th>
    <th>Rel</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.uid %></td>
    <td><%= post.post %></td>
    <td><%= post.votes %></td>
    <td><%= post.root %></td>
    <td><%= post.rel %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
<%= link_to 'Register', post_url %>
<%= link_to 'Login', new_post_path %>
